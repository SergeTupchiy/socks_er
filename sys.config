%% -*- mode: Erlang -*-
[
 {socks_er, 
  []},
 {kernel, 
  [{logger_level, info},
   {logger, 
    [{handler, default, logger_std_h,
      #{level => info, 
        formatter => 
            {logger_formatter, 
             #{template => [time," ",pid," ",msg,"\n"]}}
       }}
    ]}
  ]}
].
